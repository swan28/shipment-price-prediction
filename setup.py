from setuptools import find_packages, setup

setup(
    name="shipment-price-prediction",
    version='0.0.1',
    author='swati',
    author_email='emailid@gmail.com',
    packages=find_packages(),
    install_requires=[]
)